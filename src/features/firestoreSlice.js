import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    
}

export const firestoreSlice = createSlice({
    name: 'firestoreEntrys',
    initialState,
    reducers:{

    }
});

export const {} = firestoreSlice.actions;

export default firestoreSlice.reducer;